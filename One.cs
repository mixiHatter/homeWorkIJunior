internal class Program
{
    private static void Main(string[] args)
    {
        //Попрактикуйтесь в создании переменных, объявить 10 переменных разных типов.
        //Напоминание: переменные именуются с маленькой буквы, если название состоит из нескольких слов, то комбинируем их следующим образом - названиеПеременной. lowerCamelCase
        //Также имя всегда должно отражать суть того, что хранит переменная.
        int age;
        float money;
        bool answer;
        string message;
        byte block;
        short seaLevel;
        uint iceCreamInStock;
        double weight;
        char cipher;
        long numberOfSeatsInTheAuditorium;
        var text = "Thx 4 reading";
    }
}